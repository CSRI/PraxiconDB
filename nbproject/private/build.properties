# build properties

name=xdoclet_tutorial

src.dir=${basedir}/src
lib.dir=${basedir}/lib
bin.dir=${basedir}/bin

#xdoclet.dir = ${root.dir}/xdoclet
xdoclet.dir=${basedir}

xdoclet.lib.dir = ${xdoclet.dir}/lib
xdoclet.src.dir = ${src.dir}
xdoclet.template.dir = ${xdoclet.dir}/templates
xdoclet.jaxb.dir = ${xdoclet.dir}/templates

target.dir = ${xdoclet.dir}/target
xdoclet.gen-src.dir = ${target.dir}/gen-src
classes.dir = ${target.dir}/classes
target.lib.dir = ${target.dir}/lib
dist.dir = ${target.dir}/dist

xdoclet.force = false